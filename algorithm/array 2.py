Arr = list(map(int, input()))

dy = [-1, 1, ,0, 0]
dx = [0, 0, -1, 1,]


def IsSafe(y, x):
    if y >= 0 and y < n
    return ()








for y in range(len(dy)):
    for x in range(lend(dx)):
        for i in range(4):
            new_Y = y + dy[i]
            new_X = x + dx[i]
            if IsSafe(new_Y, new_X)
